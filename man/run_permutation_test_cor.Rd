% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/run_permutation_test_cor.R
\name{run_permutation_test_cor}
\alias{run_permutation_test_cor}
\title{Permutation test for correlation between two variables}
\usage{
run_permutation_test_cor(
  df,
  x_var,
  y_var,
  B = 1000,
  method = "pearson",
  use = "complete.obs",
  workers = 1,
  seed = 1,
  drop_duplicates = FALSE,
  save_path = NULL,
  force_new = FALSE
)
}
\arguments{
\item{df}{A data frame containing the variables of interest.}

\item{x_var}{A string specifying the name of the first variable (e.g., "info_content").}

\item{y_var}{A string specifying the name of the second variable (e.g., "n_cell_types_sig").}

\item{B}{An integer specifying the number of permutations to perform (default is 1000).}

\item{method}{A string specifying the correlation method to use ("pearson", "spearman", etc.; default is "pearson").}

\item{use}{an optional character string giving a
    method for computing covariances in the presence
    of missing values.  This must be (an abbreviation of) one of the strings
    \code{"everything"}, \code{"all.obs"}, \code{"complete.obs"},
    \code{"na.or.complete"}, or \code{"pairwise.complete.obs"}.}

\item{workers}{An integer specifying the number of parallel workers to use (default is 1).}

\item{seed}{An integer specifying the random seed for reproducibility (default is 1).}

\item{drop_duplicates}{A logical indicating whether to drop duplicate (x_var, y_var) pairs before analysis (default is TRUE).}

\item{save_path}{File path to cache results.}

\item{force_new}{Ignore cached results.}
}
\value{
A data table containing the observed correlation, empirical p-value, and null distribution statistics.
}
\description{
This function performs a permutation test to assess the significance of the correlation
between two variables in a data frame. It computes the observed correlation, generates
a null distribution of correlations by permuting one of the variables, and calculates
an empirical p-value.
}
\examples{
# Example usage:
df <- data.frame(
  info_content = rnorm(100),
  n_cell_types_sig = rnorm(100)
)
result <- run_permutation_test_cor(
df, x_var="info_content", y_var="n_cell_types_sig", B = 1000)
print(result)
}
